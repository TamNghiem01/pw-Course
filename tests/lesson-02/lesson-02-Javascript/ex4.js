const chieuCao = 52;

if (chieuCao > 100) {

    console.log(`Cân nặng lý tưởng, tối đa và tối thiểu của bạn lần lượt là: ${(chieuCao-100)*9/10} kg, ${chieuCao-100} kg, ${(chieuCao-100)*8/10} kg`);

} else {

    console.log("Công thức chỉ áp dụng cho chiều cao trên 100 cm");

}